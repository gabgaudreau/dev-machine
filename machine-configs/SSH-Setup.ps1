# run the following command
# when asked for filename, just hit enter to save to default location c:/users/user/.ssh
# set passphrase
# open .pub file
# copy and paste the value into github
# one first use, add to known hosts

ssh-keygen -t ed25519 -C "gabriel.gaudreau23@hotmail.com"